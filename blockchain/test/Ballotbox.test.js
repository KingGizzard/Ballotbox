const { assert } = require('chai');
const truffleAssert = require('truffle-assertions');

const Ballotbox = artifacts.require('Ballotbox');

contract('Ballotbox', async accounts => {
  before(async () => {
  });

  describe('function()', async () => {
    it('"function action"', async () => {
    });
  },
)})